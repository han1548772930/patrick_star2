import { PreviewWindow } from "preview.slint";
import { PinWindow } from "pin.slint";
import { SettingsWindow } from "settings.slint";
import { AppTray } from "tray.slint";

export { PreviewWindow, PinWindow, SettingsWindow, AppTray }
